<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <%= typeof title !== 'undefined' ? title : 'Tech4U - Đăng nhập/Đăng ký' %>
    </title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!-- Sử dụng file CSS riêng cho auth thay vì style chung -->
    <link rel="stylesheet" href="/css/auth.css" />
  </head>
  <body>
    <div class="container-fluid auth-container p-0">
      <div class="auth-form">
        <div class="form-container">
          <div class="logo-container">
            <a href="/">
              <div class="d-flex align-items-center">
                <i
                  class="fas fa-laptop text-primary me-2"
                  style="font-size: 1.8rem"
                ></i>
                <span class="h3 mb-0 text-primary fw-bold">Tech4U</span>
              </div>
            </a>
          </div>

          <%- include('../partials/messages') %> <%- body %>

          <div class="text-center mt-4">
            <p class="text-muted small">
              &copy; <%= new Date().getFullYear() %> Tech4U. Tất cả quyền được
              bảo lưu.
            </p>
          </div>
        </div>
      </div>
      <div class="auth-banner">
        <!-- Banner content can be added here -->
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/script.js"></script>
  </body>
</html>
